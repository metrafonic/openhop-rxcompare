services:
  rxcompare:
    image: ghcr.io/metrafonic/openhop_compare:latest
    container_name: openhop-rxcompare
    env_file: .env
    environment:
      TZ: ${TZ:-UTC}
    ports:
      - "${LISTEN_PORT:-8090}:8080"
    restart: unless-stopped
